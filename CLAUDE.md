# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

Native Android PDF reader for blind users (Kotlin, Jetpack Compose, Media3), version 0.10.3.

**The reference device is a Samsung Galaxy S25, Android 16 / One UI 8.5, Samsung TalkBack 16.2, Vocalizer speech engine, mostly a Bluetooth speaker** (`docs/decisions/0002-target-device-samsung-s25.md`). Only a Pixel emulator with Google TTS is available here, so the emulator proves logic and regressions, never that something works on the target. It is still unverified whether Vocalizer supports `synthesizeToFile`, which the whole audio pipeline depends on; the in-app diagnosis in the settings dialog exists to answer that remotely.
 Text PDFs and EPUB books are imported, split into chapters, synthesized to audio with a local German Android TTS voice and played through a MediaSession. Everything is designed around TalkBack, voice commands and media keys. Android is the lead platform by decision (`docs/decisions/0001-android-first.md`); do not design for a shared iOS codebase.

Language conventions. **Everything a developer reads is English, everything the app speaks is German.**

- English: code, comments, commit messages (`fix:`, `feat:`, `docs:` prefixes), pull requests, GitHub release notes, `README.md` and every file under `docs/` except the one below. The project is public so that someone else can pick it up, including for another UI language, and German documentation would lock them out.
- German: UI strings, status messages, spoken output, voice command words in `CommandParser`, and `docs/BEDIENUNG.md`, the manual for the people actually using the app today. Keep new user-facing strings German.
- German quotations of what a test reader said stay German inside an English document, with a short English translation in square brackets after them. A translated quote is no longer evidence.

## Build and test

Requires Java 17 as Gradle JDK (daemon toolchain configured in `gradle/gradle-daemon-jvm.properties`), Android SDK 36, Build Tools 36.0.0. minSdk 26, targetSdk 36. AGP 9.4.0 / Gradle 9.6.0 with legacy-DSL compatibility flags in `gradle.properties`; do not remove those flags casually.

```sh
./gradlew :app:assembleDebug :app:testDebugUnitTest :app:lintDebug   # build, JVM tests, lint
./gradlew :app:connectedDebugAndroidTest                              # instrumented tests, needs device/emulator
./gradlew :app:testDebugUnitTest --tests "de.schimmilab.accessiblereader.ReaderCoreTest"
./gradlew :app:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=de.schimmilab.accessiblereader.PlaybackFocusTest
```

`.github/workflows/tests.yml` runs on every push: unit tests, lint and a debug build, plus `PdfImportTest`, `EpubImportTest`, `LibraryTest`, `AccessibilityRulesTest`, `SectionResumeTest`, `TwoVoicesTest`, `BookmarkTest`, `SleepTimerSettingTest`, `SearchFlowTest` and `PositionAnnouncementTest` on an emulator. Those ten need no speech engine; the last two get by with `SilentVoices`, a speech provider that returns silent WAV files of a fixed length and records what was asked of which voice, which is enough to test where a section starts and which voice each part went to. Everything else does, and a fresh CI emulator has no German offline voice, so do not add voice or playback tests there; they would skip or fail for reasons that say nothing about the code.

**Turn TalkBack off before an automated run, and back on for manual accessibility testing.** With it enabled, every status live-region update makes TalkBack speak and hold audio focus, so playback tests stall for minutes and time out; measured 43-79 s per test with it off against 250 s timeouts with it on. `docs/TESTING.md` has the two commands.

Pass one test class per `connectedDebugAndroidTest` invocation; a comma-separated class list only runs the first class under AGP 9. Instrumented tests that seek or wait for a chapter end must first `waitUntil { !state.preparing }`: the emulator synthesizes at roughly 30 s per part, slower than playback. Start playback in tests through the model, not by clicking "Vorlesen", because a fresh install first shows the notification permission dialog.

Debug APK: `app/build/outputs/apk/debug/app-debug.apk`.

Test layout:
- `app/src/test` (`ReaderCoreTest`): pure JVM tests for `core/ReaderModels.kt` (chunking, command parsing, audio timeline). Only place for logic that must run without a device.
- `app/src/androidTest`: `PdfImportTest`, `ReaderUiTest`, `SpeechAudioTest`, `PlaybackFocusTest`, `ProgressivePreparationTest` (synthesizes a 5-chunk chapter, takes about 3 minutes on the emulator), `MediaButtonTest` (drives the session through a second `MediaController`, the way headsets and the notification do). They need a Google-Play emulator or device with a German *offline* TTS voice installed. `SpeechAudioTest` skips via `assumeTrue` if none exists; `PlaybackFocusTest` drives a real ExoPlayer and real audio focus and can take minutes.
- `AccessibilityRulesTest` checks this project's own written rules against the real screen: every reachable control has a name, measures at least 48 by 48 dp, and no two share a name, plus headings to move by. It exists because `docs/TESTING.md` claimed for weeks that an accessibility framework was running here and none was; the first real run found five controls of 40 dp, one of them the button that closes the settings. Every `TextButton` needs `heightIn(min = 48.dp)`, the Material default is 40.
- TalkBack behaviour is verified manually on the emulator (see `docs/TESTING.md`). A Compose click test does not replace a TalkBack double-tap test, and UI automation must not run while TalkBack gestures are being tested because it suppresses other accessibility services.

## Architecture

Single module `app`, package `de.schimmilab.accessiblereader`. The pipeline is:

1. `MainActivity` receives a PDF via file picker, `VIEW` or `SEND` intent and owns the on-device `SpeechRecognizer` (API 31+, German, started only by button press).
2. `data/DocumentStore` copies the PDF to a temp file, extracts text per page with PDFBox-Android, hands any page that comes back blank to `PageOcr` for text recognition, maps PDF bookmarks to chapters, or groups pages into sections when there are none, stores the result as JSON in private app storage and deletes the temp copy. No headings are guessed. It also writes a small `<id>.meta.json` beside it; `library()` lists those side files so opening the library never parses a whole book, and heals a missing side file once. `remove()` deletes text and side file, never the user's PDF.
3. `ReaderViewModel.play()` splits the chapter with `TextChunks.split` (max 1000 UTF-16 chars, at sentence/word boundaries), calls `SpeechProvider.synthesize` per chunk, and builds one Media3 `MediaItem` per chunk. Each item carries extras `document`, `chapter`, `voice`, `duration`. Chunk 0 is always the spoken chapter intro from `ChapterAnnouncement`, so saved item indexes stay stable. Playback starts once at least `MIN_LEAD_MS` (12 s) of audio is buffered ahead of the start position, or all chunks are ready; the remaining chunks are appended with `addMediaItem` while `ReaderState.preparing` is true. The lead exists because the ~3 s intro alone would drain into silence before the first text chunk finished synthesizing. `busy` only covers the wait before the first sound. When a fully prepared chapter ends with `playWhenReady` still true, `syncPlayer()` advances to the next chapter via `chapter(i, announce = false)` and `play(quiet = true)`; the last chapter stops and offers a restart.
4. `speech/AndroidSpeechProvider` takes an engine package (blank = system default, persisted under `engine`) — never assume the default engine, the target device's default reports zero voices via `getVoices()`. `voices()` falls back to `isLanguageAvailable` and offers `ENGINE_DEFAULT_VOICE` when an engine names none; `synthesize` then uses `setLanguage` instead of `setVoice`. The cache key contains the engine actually in use. `speech/SpeechProbe` probes one engine at a time for the diagnosis. It holds two TextToSpeech instances: one writes chapter audio to files, a second one (`say`/`isSaying`/`stopSaying`) speaks short feedback. One engine cannot speak and write a file at once, so sharing it silenced every announcement during background preparation. It synthesizes to WAV files in `cacheDir/speech`, keyed by SHA-256 of `providerId|engine|voiceId|text`. A file is only committed under its final name after its duration is verified > 0. Every German voice whose data is installed is offered, including ones that fetch their audio from the internet; those are marked and gated by `mayUseOnlineVoice`. A voice the engine flags `notInstalled` is skipped, and `SpeechReport` must apply the same rule or the diagnosis and the app contradict each other, which `VoiceListTest.theReportAndTheVoiceListAgree` guards.
5. `playback/ReaderPlaybackService` (a `MediaSessionService` with ExoPlayer) plays the items in the background and writes `chapter`, `item`, `offset`, `voice`, `finished` into SharedPreferences `"reader"` once per second, keyed by document id. Resume on next start reads these keys. The session wraps ExoPlayer in `ChapterPlayer` (a `ForwardingSimpleBasePlayer`): seek back/forward run on the chapter timeline, next/previous become custom commands `COMMAND_NEXT_CHAPTER` / `COMMAND_PREVIOUS_CHAPTER` that the ViewModel's `MediaController.Listener` turns into `chapter()` calls. It advertises `REPEAT_MODE_ALL` only so SimpleBasePlayer keeps routing "next" on the last part into `handleSeek`.

State rules worth knowing before editing `ReaderViewModel`:
- The player is the source of truth. `syncPlayer()` reads the current item's extras back and recomputes `chapter`, `positionMs`, `durationMs`, `playing` and `playbackRequested`. `preparedKey` (`docId:chapter:voiceId`) tells whether the loaded playlist matches the current selection; anything that changes document, chapter or voice must clear it and the media items.
- `ReaderState.playing` is `Player.isPlaying`. `ReaderState.playbackRequested` is `playWhenReady` outside IDLE/ENDED, where ENDED counts as "still requested" while `preparing` (the player merely ran out of appended chunks). The Play/Pause button and chapter switching key off `playbackRequested`, never `playing`. Reason: TalkBack announcements take transient audio focus and Media3 pauses briefly; if the button flipped to "Vorlesen" TalkBack would re-announce it and interrupt again. Do not add a custom resume timer; Media3 handles audio focus.
- An announcement goes either to the live region or to the book voice, never both: `screenReaderActive()` (touch exploration) decides, because TalkBack already reads the status live region and speaking too would double it. Reading the contents aloud and the voice preview always use the book voice and set no status.
- Never write to `ReaderState.status` from the background preparation loop. It is a polite live region, and every update interrupts the book under TalkBack. Anything that replaces the playlist (chapter, voice, document, clear cache) must call `stopPreparation()` first.
- 30-second jumps are computed on the absolute audio timeline across all chunk files (`AudioTimeline`), not per media item, both in the ViewModel and in `ChapterPlayer`.
- **A timeout is a `CancellationException`.** `withTimeout` throws `TimeoutCancellationException`, which every `catch (e: Exception) { if (e is CancellationException) throw e }` treats as "the user cancelled" and swallows without a word. That turned a slow voice into an app that sat there with no sound, no message and nothing to press. `AndroidSpeechProvider.synthesize` uses `withTimeoutOrNull` and raises a real error instead; the ViewModel excludes `TimeoutCancellationException` explicitly when it rethrows cancellation.
- The speed of a voice is measured while a section is prepared and shown to the listener under the voice list (`recordVoiceSpeed`, `voiceSpeedNote`). Only real syntheses count; `SpeechAudio.fromCache` keeps cache hits out of the average, otherwise every voice looks instant on a second run.
- **Do not assume synthesis outruns playback.** Measured on the emulator: the stock Google voice needs about 3 ms per character, a local neural voice (Piper Thorsten through VoxSherpa) 30 ms, and up to 115 ms while it also speaks an announcement. So `TextChunks.splitForPlayback` ramps the pieces, 250 then 500 then 1000 characters, and `AndroidSpeechProvider.synthesisBudgetMs` scales the limit with the text. Test both kinds of engine: `SynthesisSpeedTest` times every installed engine, `SlowVoiceTest` drives the whole flow with the neural one.
- **Two `TextToSpeech` instances against one engine is fine for Google, Vocalizer and Acapela, but an engine with a single native synthesizer serves them one after the other.** Both therefore go through one `Mutex` in `AndroidSpeechProvider`: `say` waits for the piece being synthesized and holds the lock until its sentence is out. Starting them at once made the book's audio wait behind the announcement, measured 40 seconds against 12, which then ran past the synthesis budget and stopped the book with a message a listener could do nothing about. `AnnouncementCollisionTest` is the guard; it fails within a minute if the two are ever let loose on the engine at the same time.
- Do not wrap Media3 listeners with Kotlin `by` delegation: Kotlin does not delegate Java default methods, so the session silently loses every state callback. Use `ForwardingSimpleBasePlayer` for player-level behaviour changes.
- **The section loop lives in `playback/SectionPreparer`, not in the ViewModel**, because the service needs the same behaviour when the app is gone. Two copies of that loop would be two sets of the subtleties it holds, and those have cost this project several releases. The ViewModel passes a `SectionProgress` for its UI state; the service passes an empty one.
- **The headset buttons for the next section work without the app too.** `ChapterPlayer` asks the app while `KEY_UI_ALIVE` is set, because the app also has to move its own screen along, and otherwise the service prepares the section itself through the same `prepareSection`.
- **A `CancellationException` is an `IllegalStateException`.** So is a timeout, which is a `CancellationException`. A `catch (e: IllegalStateException)` meaning "something went wrong" therefore swallows or retries the user pressing cancel. This has bitten twice; `ReaderCoreTest.aCancellationIsAnIllegalStateException` writes it down as a runnable fact, and `AndroidSpeechProvider.synthesize` catches cancellation first and rethrows it before its retry.
- **A timeout is not retried.** `VoiceTooSlowException` is its own kind precisely because it is an `IllegalStateException` like everything else here, and the retry below would otherwise make a listener who is already waiting wait the whole budget a second time: a thousand characters at the 300 ms each the budget allows is five minutes of silence, then five more. The error it raises names the voices the app has measured as keeping up (`slowVoiceMessage`, `voicesThatKeepUp`), because the old message said "Bitte eine andere Stimme wählen" and the listener it reached answered "Ich glaube, ich bin blöd".
- `synthesize` tries twice. An engine answers with an empty file now and then, right after another client of it has shut down, which is exactly what the app being swiped away does mid-book. Seen in a real run; the second attempt produced the audio. Do not remove it without checking `ReaderService` warnings during `KeepsReadingWithoutTheAppTest`.
- **When the app dies, the service continues the book.** `ReaderPlaybackService` watches for a finished section and prepares the next one itself, reconstructing everything from the current media item: document id, section, voice. The text comes from the same `DocumentStore`, so nothing is handed across a Binder. The ViewModel sets `KEY_UI_ALIVE` in its init and clears it in `onCleared`, and the service only steps in when that flag is false, so the common path with a live app is unchanged.
- Resuming where the listener stopped is the user's one named requirement (kept privately, see `docs/COLLABORATION.md`), guarded by `ResumePositionTest`. An empty buffer reports the same player state as a real chapter end, so the service writes `finished` only while `ReaderPlaybackService.KEY_PREPARING` is unset; a chapter wrongly marked finished restarts instead of resuming.
- **A novel can be read with two voices**, a narrator and one for everything in quotation marks (`core/Narration.kt`). What identifies a prepared section is the whole `VoiceCast`, not one voice: its `key` goes into every media item as the `voice` extra and into the saved position, and with one voice that key is exactly the voice id, so nothing saved by an earlier version changes meaning. The voice a single part was spoken by is the separate `partVoice` extra. **Who** speaks is never guessed: measured over all of *Effi Briest*, four out of five lines of dialogue carry no "sagte er" or "sagte sie" at all, so a male and a female voice would be wrong most of the time. Both voices come from one engine, because one provider speaks to one engine, and both play at the speed set for the chosen voice.
- **"Wo bin ich?" says where in the book, not only where in the section.** A listener has no scroll bar to feel and no thumb in the pages, so it is the only way to know how much is left. The share is counted in characters, because sections differ in length by a lot, and said in words someone can picture ("etwa ein Drittel", `bookProgressLabel`). The estimate of the time left is only given once the app has measured how fast this voice reads (`remainingTimeLabel`), because an estimate at a rate nobody measured would be a made-up number spoken with confidence.
- **The reader never guesses which character is speaking** (`docs/decisions/0006-not-guessing-who-speaks.md`). Measured against the 323 lines of *Effi Briest* whose narration says who spoke: taking turns is right 46 % of the time, worse than a coin toss, and carrying the last speaker forward 68 %, which means every third line in the wrong voice. One voice for all the characters is the honest version.
- **A search hit is a place playback can start at** (`core/Search.kt`). It carries the section and the part, counted the way the player counts, so reaching one is the same machinery as a bookmark. The part is derived from where the hit sits in the text, which only works because the parts are cut from the text alone — with a dialogue voice set the section is cut differently and `search` has to be told, which is why it takes the cast into account. Both sides are folded by `searchable` (umlauts written out, punctuation dropped), so "fuer" finds "für"; `CommandParser` hands a spoken query on **unfolded**, because its own folding turns "Brücke" into "brucke", which is in no German book. That was caught by a test, not by a listener.
- **The sleep timer lives in the playback service**, not in the ViewModel (`KEY_SLEEP_UNTIL`, `KEY_SLEEP_AT_SECTION_END`). Someone who sets a timer and puts the phone down is precisely the case where the app goes away, and the service is then the only part left that could stop anything. It ticks in the same once-a-second runnable that saves the position: it fades the volume over the last 20 seconds (`core/sleepVolume`), pauses when the time is up, and with no timer set it forces the volume back to 1, because a listener left with a silent reader has no way to find out why. "Am Ende des Abschnitts" has to be handled twice, in `continueWithoutTheApp` and in the ViewModel's auto-advance, because both know how to continue a book and both therefore had to learn to stop.
- **A bookmark is a resume position with a name** (`core/Bookmarks.kt`). `mark()` writes the same three numbers the resume position uses, and `goToBookmark` hands them back through the very same preferences keys, so returning to one behaves exactly like continuing after the app was closed, including what happens when the voice has changed since. A new bookmark within 20 seconds of an existing one in the same section replaces it: pressing the button twice because you could not tell whether it worked is what happens without sight, and two near-identical entries in a spoken list are worse than one.
- Position, chapter and voice are read from SharedPreferences on resume. `core/resumePoint` decides where a section starts: a section is cut into parts by its text alone, so another voice keeps the part someone was listening to and only gives up the seconds inside it, which belong to the old voice. At most one part is repeated instead of the whole section.

## Hard constraints

- Scanned pages go through `data/PageOcr`: rendered with `PdfRenderer`, recognized with the bundled ML Kit Latin model, about 1.2 seconds per page. Render bigger only with a measurement in hand: between 764 and 1200 pixels of page width the recognized text was character for character identical while the time per page grew by a quarter. The model is inside the APK and the app manifest removes the `INTERNET` permission the library declares with `tools:node="remove"`. Verify that with `aapt2 dump permissions` after touching dependencies; a silently merged network permission would break the promise the whole project rests on.
- Voices that fetch their audio from the internet are offered by the app but reached only by the speech engine, in its own process, so the app itself stays offline. `ACCESS_NETWORK_STATE` is in the manifest to tell Wi-Fi from mobile data, and it grants no network access; `core/Library.kt` holds the policy (`mayUseOnlineVoice`), the ViewModel checks it before preparing anything (`decisions/0005-online-voices-of-the-speech-engine.md`).
- The app has no `INTERNET` permission and must not gain one silently. Cloud voices (Chirp 3 HD, Neural2) are planned only as additional `SpeechProvider` implementations, and each needs explicit consent, a cost limit and key handling before integration.
- Voice commands never fall back to a cloud recognizer. If German offline speech data is missing, the labelled buttons are the fallback.
- PDFs and generated audio stay on the device in private storage. No blanket storage permission.
- **Each voice keeps its own speed**, under `speed.rate.<voiceId>`. Voices differ by half again at their own natural rate: measured on an emulator, a local neural voice reads at about 204 words a minute where the stock voice reads at 132, and a listener had to slow the first one down. One number for all of them means changing the voice silently changes the speed. The old single `speed` key stays as the starting point for a voice never adjusted, and `speed()` must not write it, or one voice drags the others along.
- The reading speed moves in steps of `SPEED_STEP` (0.1) and every change is spoken through the status line. A quarter step was unusable: from normal the next one down was noticeably slow and the next one up noticeably fast. A screen reader keeps its focus on the button it pressed and never reads the value that changed, so the app has to say it.
- Every core action must be reachable via TalkBack, voice command (`CommandParser` in `core/ReaderModels.kt`) and media keys, never only by gesture.
- **A PDF without bookmarks is grouped, not split per page.** `groupPagesIntoSections` collects pages into sections of about `SECTION_TARGET_CHARACTERS` (10,000). One section per page means one interruption per page, plus the spoken section number before each.
- **The next section is synthesized into the cache while the current one plays** (`prefetchNext`). Without it the sound stops at every section change for as long as the next section needs to reach `MIN_LEAD_MS`: measured 9 seconds with the stock voice and 21 with a local neural one. With it, 0.3 to 1 second for the stock voice. A voice that synthesizes at about the speed of playback has no spare capacity to work ahead, so it stays slower; that is the voice, not the code. The prefetch must stay out of `preparing`, because that flag gates automatic continuation.
- **Measure silence, not the distance between section changes.** An earlier version of `PageSectionsTest` timed how long it took for the chapter index to change again, which contains the audio itself, and reported 8 to 12 seconds of silence that did not exist in that form. Watch `playing` going false and true again.
- **An EPUB's own table of contents is not always a shape worth listening to.** Books routinely put fifty chapters into four files and tell them apart by the anchor alone, so `Epub.split` cuts a document where its contents point into it. Others name 565 entries for one book, so `groupTitledSections` starts a section only once the current one has grown to `SECTION_TARGET_CHARACTERS`. Others again carry page numbers as titles, so `sectionTitle` replaces a title that is only a number. And one real book is a single file of half a million characters, which `splitOversized` cuts into parts. Every one of these came from a real book; none would have shown up on a generated fixture.
- **Copy protection and font scrambling are not the same thing.** Two books in the test collection carry a `META-INF/encryption.xml` and read perfectly, because the IDPF and Adobe font schemes only scramble the embedded fonts. `Epub.encryptedPaths` ignores those two algorithms and reports the rest, and a book whose reading order is encrypted is refused as copy-protected rather than as damaged. The difference decides where a listener goes looking: for a broken download that is not broken, or for the shop's own app.
- **EPUB is the better source and is parsed in `core/Epub.kt`**, pure text in and text out so it runs without a device. An EPUB states its reading order and nearly always its chapters, so nothing is guessed: no page numbers to strip, no hyphens broken across pages, no recognition. Use `TextChunks.cleanBlock` for it, never `clean`, because dropping a lone number would eat a chapter heading that is simply "1". `ReaderDocument.paged` is false for an EPUB and the screen then says nothing about pages, because the twelfth file in an archive is not page twelve.
- **A page with real columns is read one column at a time** (`core/PageColumns.kt`, `data/PageColumnReading.kt`). Text extraction sees a page as lines across its full width, which splices the right column into the sentences of the left. `ColumnAwareStripper` writes down the geometry of every line during the ordinary extraction pass, so recognising such a page costs no second reading; only a page that really has columns is read again, by area. The rules are deliberately reluctant and every one of them was put there by a page that broke without it: not one line may touch the gutter, each column needs at least eight rows of its own, the columns have to run at least 40 % of the page height, and a band of lines spanning the full width stays whole. Without the row and height rules a table of contribution rates was taken for two columns and its numbers were torn from the rows they belong to. Measured over 332 pages of eight real books: ten pages of a product catalogue are read differently and the other 322 come out character for character as before, which `PageLayoutSurveyTest` asserts.
- Text cleanup happens once, per page, in `TextChunks.clean` and the order of its steps is load-bearing: collapse whitespace, drop the running page number, then repair hyphens. Repairing first glues the page number onto the word the page break cut in half. Words split across a page boundary are rejoined in `TextChunks.joinPages`, which is why a trailing soft hyphen survives `clean`.
- Real books are checked with `RealBooksTest`, a harness that imports whatever PDFs sit in the app's own cache folder (`cache/books`) and logs the result to the `ReaderBooks` tag. Gradle uninstalls the app after a connected run and takes that folder with it, so install both APKs with `adb install -r` and start it with `adb shell am instrument`. Generated fixtures say nothing about print typesetting.
- Limits enforced in code (`DocumentStore.MAX_BYTES` / `MAX_PAGES` / `MAX_CHARACTERS`): 200 MB / 10,000 pages / 20 M chars per PDF, raised twice by real books, most recently for one of 3700 pages. Raising the number is free and surviving it is not, so `LargeBookTest` imports a book of that size: measured 148 seconds and 74 MB of a 192 MB heap for 7.4 million characters. Measure again before raising it further. Contents read-aloud capped at 20 entries.
- The audio cache is trimmed, never a wall: `AndroidSpeechProvider.trimCache` drops the least recently used files down to 80 % of `ReaderViewModel.CACHE_BUDGET_BYTES` before preparing a chapter. A cache hit touches the file so recently heard chapters count as recent. Refusing to play once the cache is full would strand a listener mid-book.

## Docs to keep in sync

- `README.md` "Grenzen dieser Version" and `docs/ARCHITECTURE.md` describe the current version; update them when behaviour or limits change.
- Each tested APK gets a `docs/TEST-<version>.md` protocol (environment, results, open items). Bump `versionCode`/`versionName` in `app/build.gradle.kts` together.
- Platform or product decisions go to `docs/decisions/NNNN-*.md`. `docs/PLAN.md` is the full product target, not a list of finished features.
